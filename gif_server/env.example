NODE_ENV=development
PORT=3000

# 本地文件存储根目录（容器内）
DATA_DIR=/data

# 测试环境建议 7 天：168；生产建议 48
RETENTION_HOURS=168

# 上传限制
MAX_UPLOAD_MB=200

# 业务限制
MAX_CLIP_DURATION_S=20

# FFmpeg 参数默认值（可按需调整）
# DEFAULT_FPS: 默认帧率（1-24），值越小文件越小但动画越不流畅
#   - 10 fps：适合大多数场景，文件大小和流畅度平衡（默认）
#   - 15 fps：更流畅，适合快速动作
#   - 24 fps：最流畅，但文件最大
# DEFAULT_WIDTH: 默认宽度（120-1280），值越大文件越大但画质越好
#   - 480：适合小尺寸显示，文件小（默认）
#   - 720：中等尺寸，平衡画质和文件大小
#   - 1280：720P 高清，文件较大但画质最好
# 
# 720P 建议: DEFAULT_WIDTH=1280, DEFAULT_FPS=15
# 480P 建议: DEFAULT_WIDTH=480, DEFAULT_FPS=10（默认值）
DEFAULT_FPS=10
DEFAULT_WIDTH=480

# FFmpeg 可执行文件路径（可选）
# Windows: FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
# Linux/Mac: FFMPEG_PATH=/usr/bin/ffmpeg
# 如果不设置，将使用系统 PATH 中的 ffmpeg
FFMPEG_PATH=ffmpeg
