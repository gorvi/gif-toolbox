NODE_ENV=development
PORT=3000

# 本地文件存储根目录（容器内）
DATA_DIR=/data

# 测试环境建议 7 天：168；生产建议 48
RETENTION_HOURS=168

# 上传限制
MAX_UPLOAD_MB=200

# 业务限制
MAX_CLIP_DURATION_S=20

# FFmpeg 参数默认值（可按需调整）
DEFAULT_FPS=10
DEFAULT_WIDTH=480

# FFmpeg 可执行文件路径（可选）
# Windows: FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
# Linux/Mac: FFMPEG_PATH=/usr/bin/ffmpeg
# 如果不设置，将使用系统 PATH 中的 ffmpeg
FFMPEG_PATH=ffmpeg
